/* eslint-disable @typescript-eslint/no-empty-function */
import { test } from '@playwright/test';

test('should appear on the page after clicking on "Add Item"', () => {});
test('should appear in the "Unpacked Items" list', () => {});
test('should show items that match whatever is in the filter field', () => {});
test('should hide items that do not match whatever is in the filter field', () => {});
test('should remove all of the elements from the page', () => {});
test('should have a remove button on an item that removes it from the page', () => {});
test('should empty out the "Packed" list', () => {});
test('should empty out all of the items in the "Unpacked" list', () => {});
test('should move an individual item from "Unpacked" to "Packed"', () => {});
